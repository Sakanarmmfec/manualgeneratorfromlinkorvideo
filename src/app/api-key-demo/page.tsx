import { ApiKeyDemo } from '@/components/api-key/ApiKeyDemo';

export default function ApiKeyDemoPage() {
  return <ApiKeyDemo />;
}

export const metadata = {
  title: 'API Key Management Demo - Thai Document Generator',
  description: 'Demo of the API key management system with automatic fallback support'
};